<%= form_with(model: faphra, local: true) do |form| %>
  <% if faphra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faphra.errors.count, "error") %> prohibited this faphra from being saved:</h2>

      <ul>
        <% faphra.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
