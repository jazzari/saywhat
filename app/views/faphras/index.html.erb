<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>	
        <%= render 'feed' %>
        <%= render 'who_to_follow' %>
      <% else %>
        <%= render 'feed' %>
        <%= render 'trends' %>
      <% end %>
    </div>
  </div>
</section>


